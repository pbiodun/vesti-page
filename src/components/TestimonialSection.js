import React from "react";

const TestimonialSection = ({ fullname, date, desc, image }) => {
  return (
    <div>
      {/* This Component would be used to make the testimonial card more usable */}
    </div>
  );
};

export default TestimonialSection;
