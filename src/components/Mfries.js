const Mfries = (props) => {
  return (
    <div className="container mx-auto text-center">
      <div className="">
        {props.title}
        <p className="text-[#2B5219] text-xl">{props.heading}</p>
      </div>
    </div>
  );
};

export default Mfries;
